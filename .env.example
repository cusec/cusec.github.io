# Canonical public origin used for canonical tags, hreflang, sitemap, and robots.
# Must be the host that serves HTTP 200 (no redirect). Production: https://www.cusec.net
NEXT_PUBLIC_SITE_URL=https://www.cusec.net
